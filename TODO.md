# TODO

Documentation gaps identified by the Documentation Audit and not yet created:

- docs/integrations/ — Twilio webhooks, ElevenLabs configuration (dynamic variables and defaults, MCP tool re-sync, post-call webhook), Google OAuth notes (Testing-mode 7-day token expiry), n8n credential map.
- BUSINESS_CONFIG.md — full schema of the Business Config node and the three-clone update rule.
- Multi-vehicle support decision — the data model documents the single-vehicle-per-customer limitation; decide whether to lift it.
- Keep production/ artifacts synchronized on every deploy (see OPERATIONS.md).
